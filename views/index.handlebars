<h1>Burgers!</h1>

<h2>Burgers to be eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eliminated!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Type:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Eliminated:</label><br>
    <input type="radio" name="devoured" value="1" checked> Eliminated<br>
    <input type="radio" name="devoured" value="0"> Not Eaten Yet
  </div>

  <button type="submit">Add Burger</button>
</form>
